parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcsfixer:
            config_file: ~
            config: ~
            fixers: []
            level: psr2
            verbose: true
        phplint: ~
        securitychecker:
              lockfile: ./composer.lock
              format: ~
              end_point: ~
              timeout: ~
              run_always: false
