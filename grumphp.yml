parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcsfixer2:
            config: .php_cs
        phplint: ~
        securitychecker:
              lockfile: ./composer.lock
              format: ~
              end_point: ~
              timeout: ~
              run_always: false
